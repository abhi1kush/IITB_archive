e =True                 # low <= e
if e:			
	x = y		# y <= e * x
	z = u           # u <= b * z
	while e2 :
		a = b   # b <= a * e2 * e
		b = c   # c <= b * e * e2
else:
	p = q           # q <= p * e
	r = s           # s <= r * e
	
